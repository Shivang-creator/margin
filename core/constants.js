// Deterministic core constants. Pure data only; no network, DOM, environment, randomness,
// or imports reaching outside this directory.

export const LEDGER_VERSION = "1.0";
export const OVERLAP_THRESHOLD = 0.5;
export const MIN_QUOTE_TOKENS = 3;
export const MAX_NOTES_CHARS = 20000;
